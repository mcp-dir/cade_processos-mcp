# Ferramentas

CADE: Processos expõe 1 ferramenta (todas somente leitura).

### 1. `cade_processos_consultar`
**Input**: `processo` (opcional), `query` (opcional), `interessado_remetente` (opcional), `pagina` (opcional), `tipo_pesquisa` (opcional)

CADE: Processos, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar CADE: Processos
```
